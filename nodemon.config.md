# nodemon.config.md

- `watch`: surveille les changements dans le dossier `src`
- `ext`: redémarre si des fichiers `.ts` sont modifiés
- `exec`: lance le projet avec `ts-node`, en chargeant les variables d’environnement via `dotenv/config`
