# package.script.md

- `start`: lance l'app compilée avec JavaScript avec `node dist/index.js`
- `dev`: démarre le serveur en mode developpement et redémarre à chaque modification d'un fichier `.ts`
- `build`: compile les fichiers TypeScript en fichiers JavaScript avec `tsc` avec les options dans `tsconfig.json`
